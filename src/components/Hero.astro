---
// This component creates the dynamic, compositional hero section for the homepage.
---
<section class="relative min-h-screen w-full flex items-center justify-center p-6 md:p-12 overflow-hidden">
    <!-- This container uses absolute positioning to place elements in a compositional layout -->
    <div class="relative w-full max-w-7xl h-[80vh]">

        <!-- Main Title (Centered) -->
        <div class="absolute inset-0 flex flex-col items-center justify-center">
            <h1 class="text-6xl md:text-9xl lg:text-[9rem] font-bold tracking-tighter uppercase text-center text-white/90 leading-none">
                Behelit
            </h1>
            <h1 class="text-6xl md:text-9xl lg:text-[9rem] font-bold tracking-tighter uppercase text-center text-white/90 leading-none -mt-2 md:-mt-4">
                Systems
            </h1>
        </div>

        <!-- Core Services (Top Left) -->
        <div class="absolute top-0 left-0">
            <p class="text-sm text-gray-400 uppercase tracking-widest">
                (01) Hardware<br/>
                (02) Physical<br/>
                (03) Social<br/>
                (04) Defense
            </p>
        </div>

        <!-- Bottom row container using Flexbox to prevent overlap -->
        <div class="absolute bottom-0 left-0 right-0 w-full flex justify-between items-end">
            <!-- Intro Text (Bottom Left) -->
            <div>
                 <div class="text-gray-400 text-sm max-w-sm">
                    <p class="font-bold text-white mb-2 uppercase tracking-wider">Hidden in Plain Sight</p>
                    <p>
                        A premier Malaysian cybersecurity firm engineering unyielding resilience by targeting critical vulnerabilities in hardware, software, and the human element.
                    </p>
                </div>
            </div>

             <!-- Contact Info (Bottom Right) -->
            <div class="text-right">
                 <p class="text-sm text-gray-400 uppercase tracking-widest">
                    Say Hello At<br/>
                    <a href="mailto:hello@behelitsystems.com" class="font-bold text-white hover:text-gray-300 transition-colors">hello@behelitsystems.com</a>
                </p>
            </div>
        </div>
    </div>
</section>